import './sass/style.scss';
import pasteLayout from './keyboard';

// const WRAPPER = new Elem('div', 'wrapper', '');
// WRAPPER.paste(document.body);

// const title = new Elem('h1', 'title', 'RSS Virtual Keyboard');
// title.paste(document.querySelector('.wrapper'));
pasteLayout();
